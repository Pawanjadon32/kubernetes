apiVersion: apps/v1
kind: Deployment
metadata:
  name: useless
spec:
  replicas: 2
  selector:
    matchLabels:
      app: useless
  template:
    metadata:
      labels:
        app: useless
    spec:
      containers:
        - name: useless
          image: alok1408/fend:cp
          ports:
            - containerPort: 80
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 5
      maxUnavailable: 5